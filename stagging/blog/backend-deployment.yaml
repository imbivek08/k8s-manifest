apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-backend
  namespace: staging
  labels:
    app: blog-backend
    environment: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blog-backend
  template:
    metadata:
      labels:
        app: blog-backend
        environment: staging
    spec:
      containers:
        - name: backend
          image: imbivek01/postpluse-backend:v5
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "staging"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
